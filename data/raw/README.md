# Raw Data

Raw data is read-only after import.

Do not overwrite, normalize, or edit raw data without documenting the source, date, license/status, and reason for the change.

Current raw data:

- `LSI_ivtff_0d.txt` - IVTFF EVA interlinear transcription used by `exp-001`.

See `datasets/voynich_sources.md` and `datasets/datasets.md` for provenance and limitations.
